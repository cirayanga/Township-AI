<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Township AI Business Assistant</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 2rem;
      background: #f4f4f4;
    }
    h1, h2 {
      color: #333;
    }
    section {
      background: #fff;
      padding: 1rem 1.5rem;
      margin-bottom: 1.5rem;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    input {
      width: 100%;
      padding: 0.5rem;
      margin-top: 0.4rem;
      margin-bottom: 1rem;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 1rem;
    }
    button {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 0.6rem 1.2rem;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1rem;
    }
    button:hover {
      background-color: #0056b3;
    }
    pre {
      background-color: #eaeaea;
      padding: 1rem;
      border-radius: 6px;
      white-space: pre-wrap;
      word-wrap: break-word;
    }
  </style>
</head>
<body>
  <h1>🚀 Township AI - Business Assistant</h1>

  <!-- Business Plan Form -->
  <section>
    <h2>📄 Generate Business Plan</h2>
    <form action="/generate" method="POST">
      <input type="text" name="idea" placeholder="Business idea" required />
      <input type="text" name="location" placeholder="Your township" required />
      <button type="submit">Generate Plan</button>
    </form>
  </section>

  <!-- Marketing Content Form -->
<form action="/register-help" method="POST">
  <input type="text" name="country" placeholder="Country" required />
  <button type="submit">Get Guidance</button>
</form>


  <!-- Customer Communication Form -->
  <section>
    <h2>💬 Customer Message</h2>
    <form action="/message" method="POST">
      <input type="text" name="context" placeholder="Message context" required />
      <button type="submit">Generate Message</button>
    </form>
  </section>

  <!-- Invoice Creation Form -->
  <section>
    <h2>🧾 Create Invoice</h2>
    <form action="/invoice" method="POST">
      <input type="text" name="details" placeholder="Item, quantity, price, recipient" required />
      <button type="submit">Generate Invoice</button>
    </form>
  </section>

  <!-- Business Registration Guidance Form -->
  <section>
    <h2>📋 Business Registration Help</h2>
    <form action="/register-help" method="POST">
      <input type="text" name="country" placeholder="Country" required />
      <button type="submit">Get Guidance</button>
    </form>
  </section>

  <!-- Display AI Response -->
  <% if (typeof response !== 'undefined' && response) { %>
    <section>
      <h2>🧠 AI Response:</h2>
      <pre><%= response %></pre>
    </section>
  <% } %>
</body>
</html>





